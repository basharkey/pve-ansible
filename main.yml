---
- hosts: all
  become: yes

  pre_tasks:
    - name: Load playbook config
      include_vars: "{{ item }}"
      with_fileglob:
        - "{{ playbook_dir }}/config.yml"

  roles:
    - postfix
    - smartd
    - zfs-auto-snapshot
    - zfs-scrub
